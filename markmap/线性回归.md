一、线性回归
模型定义
假设目标值（如房价）与特征（如面积、房龄）呈线性关系，模型表达式为：
y
^
=
w
T
x
+
b
y
^
​
 =w 
T
 x+b
其中，
w
w 是权重，
b
b 是偏置项，用于调整特征加权和的基础偏移34。

损失函数
使用均方误差（MSE）衡量预测值与真实值的差距：
L
(
w
,
b
)
=
1
2
n
∑
i
=
1
n
(
y
^
i
−
y
i
)
2
L(w,b)= 
2n
1
​
 ∑ 
i=1
n
​
 ( 
y
^
​
  
i
​
 −y 
i
​
 ) 
2
 
引入 1/2 系数简化求导后的计算34。

参数优化

解析解：通过正规方程直接求解最优参数，公式为 
w
∗
=
(
X
T
X
)
−
1
X
T
y
w 
∗
 =(X 
T
 X) 
−1
 X 
T
 y，但仅适用于小规模数据（矩阵逆计算复杂度高）15。

梯度下降：迭代更新参数，沿损失函数负梯度方向调整权重和偏置。实际中多采用小批量随机梯度下降（随机抽取小批量样本计算梯度），平衡计算效率与收敛稳定性14。

矢量化加速
使用矩阵运算替代循环（如 a + b 替代逐元素相加），可显著提升计算速度（示例中加速近百倍），减少代码复杂度15。

实现步骤

合成数据生成：添加噪声模拟真实场景。

数据加载：随机打乱并按批次读取数据（如 DataLoader）。

参数初始化：随机初始化权重，偏置初始化为零。

训练循环：逐轮次计算损失、反向传播更新参数45。